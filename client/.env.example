# Base URL of the backend API.
# - In DEV (vite): leave EMPTY — requests are proxied via vite.config.js (/api -> http://localhost:3001)
# - In PROD: set to your deployed backend URL, e.g. https://vibrationcheck-api.onrender.com
VITE_API_URL=
